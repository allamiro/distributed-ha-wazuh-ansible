---
# roles/common/handlers/main.yml
# (No handlers: DNS/gateway changes are applied inline in network.yml via an
# async NetworkManager restart + wait_for_connection, because restarting NM
# drops the SSH session and a normal handler would die with it.)
