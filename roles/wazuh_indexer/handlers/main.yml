---
# roles/wazuh_indexer/handlers/main.yml

- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart wazuh-indexer
  ansible.builtin.service:
    name: "{{ indexer_service }}"
    state: restarted
