---
# Wazuh dashboard — overridable knobs.
# Connection lists (indexer URLs, manager API host) are derived in
# roles/wazuh_dashboard/vars/main.yml from the inventory groups.
dashboard_port: 443
dashboard_server_host: "0.0.0.0"
